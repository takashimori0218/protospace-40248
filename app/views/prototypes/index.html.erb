<main class="main">
  <div class="inner">
    <%= render partial: 'prototype', collection: @prototypes, as: :prototype %>
    <% ログインしているときは以下を表示する %>
      <% <div class="greeting"> %>
        <% if user_signed_in? %>
          <%= link_to "こんにちは  #{current_user.name}さん", class: :greeting__link%>
        <% end %>
      <% </div> %>
    <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
  </div>
</main>
